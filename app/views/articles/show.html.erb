<div>
  <strong>Title:</strong>
  <%= @article.title %>
</div>

<div>
  <strong>Body:</strong>
  <%= raw @article.body %>
</div>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
